import 'package:flutter/material.dart';

class PageViewFavourite extends StatefulWidget {
  const PageViewFavourite({Key? key}) : super(key: key);

  @override
  _RedState createState() => _RedState();
}

class _RedState extends State<PageViewFavourite> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
