import 'package:flutter/material.dart';

class PageViewSearch extends StatefulWidget {
  const PageViewSearch({Key? key}) : super(key: key);

  @override
  _PageViewSearchState createState() => _PageViewSearchState();
}

class _PageViewSearchState extends State<PageViewSearch> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
